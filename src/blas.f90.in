module blas
    use, intrinsic :: iso_c_binding
    private
    public :: saxpy

    include "cblas.fi"

contains

    subroutine saxpy ( n, alpha, x, incx, y, incy )
    implicit none
        integer :: n, incx, incy
        real    :: alpha
        real, target :: x(*), y(*)

        call cblas_saxpy( int(n, @BLAS_SIZE_T@), alpha, &
            c_loc(x), int(incx, @BLAS_INT_T@), &
            c_loc(y), int(incy, @BLAS_INT_T@) )
    end subroutine

end module
